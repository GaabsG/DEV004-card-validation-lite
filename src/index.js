var arrayUno= "formularioTarjeta"
function validarTarjeta() { 
    var reversa= arrayUno.reverse();
    
    console.log('reversa:', reversed);
}

/* function aleatorio (min, max ) {
    return Math.floor(Math.random() * (max - min + 1) + min) */
